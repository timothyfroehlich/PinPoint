# Test environment configuration (sanitized)
# Only contains non-production, safe demo or placeholder values.

NODE_ENV="test"

# Local Postgres / Supabase (standard local dev ports)
DATABASE_URL="postgresql://postgres:postgres@localhost:54322/postgres"
DIRECT_URL="postgresql://postgres:postgres@localhost:54322/postgres"
POSTGRES_URL_NON_POOLING="postgresql://postgres:postgres@localhost:54322/postgres"

# Supabase local/demo keys (annotated as safe for scanners)
NEXT_PUBLIC_SUPABASE_URL="http://localhost:54321"
SUPABASE_URL="http://localhost:54321"
SUPABASE_API_URL="http://localhost:54321"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="local-demo-anon-key" # gitleaks:allow
SUPABASE_SECRET_KEY="local-demo-service-role-key" # gitleaks:allow
SUPABASE_JWT_SECRET="local-demo-jwt-secret-at-least-32-chars" # gitleaks:allow

# Auth
AUTH_SECRET="test-auth-secret-at-least-32-characters-long"
NEXTAUTH_URL="http://localhost:3000"
PUBLIC_URL="http://localhost:3000"
BASE_URL="http://localhost:3000"

# OAuth test credentials (placeholders)
GOOGLE_CLIENT_ID="test-google-client-id"
GOOGLE_CLIENT_SECRET="test-google-client-secret"

# OPDB / external API placeholders
OPDB_API_URL="https://opdb.org/api"
OPDB_API_KEY="test-token" # gitleaks:allow
DEFAULT_ORG_SUBDOMAIN="apc"

# Storage / images
IMAGE_STORAGE_PROVIDER="local"

# Feature flags
NEXT_PUBLIC_ENABLE_DEV_FEATURES="true"

# CI toggle (tests may flip logic on this)
CI="false"

# Test server port (optional override)
# Chosen to avoid conflicts with common dev servers (e.g., 3000, 3001). Change as needed for your environment.
PORT="49210"