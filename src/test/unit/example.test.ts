/**
 * Example unit test
 *
 * This placeholder test ensures the test infrastructure works correctly.
 * Real unit tests will be added in Task 5: Testing Infrastructure.
 */

import { describe, it, expect } from "vitest";

describe("Test Infrastructure", () => {
  it("should run unit tests successfully", () => {
    expect(true).toBe(true);
  });

  it("should perform basic arithmetic", () => {
    expect(1 + 1).toBe(2);
  });
});
