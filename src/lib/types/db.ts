/**
 * Database Model Types (snake_case)
 *
 * Type-only namespace export of Drizzle Infer*Model types derived from schema.
 * Import with: `import type { Db } from "~/lib/types"`.
 */

export type * as Db from "~/server/db/types";
