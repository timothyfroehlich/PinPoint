/**
 * @fileoverview DEPRECATED: PinballMap API Types
 *
 * **THIS FILE IS DEPRECATED**
 * PinballMap types have been moved to `~/lib/types/external.ts` for centralization.
 * All PinballMap types are now available via `~/lib/types`.
 *
 * Migration:
 * ```typescript
 * // OLD: import type { PinballMapMachine } from "~/lib/pinballmap/types";
 * // NEW: import type { PinballMapMachine } from "~/lib/types";
 * ```
 *
 * @deprecated Use `~/lib/types` instead
 * @see ~/lib/types/external.ts - New location for external API types
 */

// This file no longer exports types to avoid ESLint violations.
// All PinballMap types are available from ~/lib/types
