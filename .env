# Safe defaults that apply to ALL environments
# This file is committed to version control and contains NO secrets

# Application configuration
PORT="49200"
IMAGE_STORAGE_PROVIDER="local"

# External API endpoints (public)
OPDB_API_URL="https://opdb.org/api"

# Default organization subdomain (for development and fallbacks)
DEFAULT_ORG_SUBDOMAIN="austin-pinball"

# Environment detection will be handled by next.config.mjs
# NEXT_PUBLIC_VERCEL_ENV is automatically set based on NODE_ENV or VERCEL_ENV