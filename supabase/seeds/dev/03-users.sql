-- DEVELOPMENT SEED (Local Dev Only) — DO NOT USE IN PROD
-- Contains dev/test users and memberships. Not for production databases.

-- User and membership creation has been moved to scripts/create-dev-users.ts
-- This file is now a placeholder to maintain the seeding sequence.

SELECT 'User and membership creation moved to scripts/create-dev-users.ts';
