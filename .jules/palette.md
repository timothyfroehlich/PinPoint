## 2025-12-20 - Accessible State Feedback
**Learning:** Icon-only buttons using `sr-only` text must update the text string (e.g., from 'Copy' to 'Copied') to reflect state changes.
**Action:** When implementing toggle or action buttons with visual state changes, ensure the screen reader text also updates dynamically.
