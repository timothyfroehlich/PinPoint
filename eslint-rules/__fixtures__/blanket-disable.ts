/* eslint-disable -- fixture: a blanket disable, which names no rules and so turns off the restricted three too */

// Deliberately its own file: a blanket disable applies to everything after it,
// so pairing it with other cases in one fixture would tell us nothing about
// which report came from which directive.
export const blanketDisabled = 1;
