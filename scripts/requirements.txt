# PinPoint Python workflow dependencies for scripts/tests.
# Python runtime: exact pin in mise.toml, target py312 in ruff.toml.
# pytest is the test runner dependency for scripts/tests/.
# pytest-xdist distributes test execution across available CPU cores.
# pyyaml is the YAML parser dependency for scripts/check_skill_frontmatter.py.
pytest==9.0.3
pytest-xdist==3.8.0
pyyaml==6.0.3

