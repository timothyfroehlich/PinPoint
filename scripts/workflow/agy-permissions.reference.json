{
  "permissions": {
    "allow": [
      "read_file(*)",
      "command(git log)",
      "command(git show)",
      "command(git diff)",
      "command(git blame)",
      "command(git status)",
      "command(git ls-files)",
      "command(git rev-parse)",
      "command(git merge-base)",
      "command(git describe)",
      "command(git shortlog)",
      "command(git grep)",
      "command(git cat-file)",
      "command(git config)",
      "command(pwd)",
      "command(ls)",
      "command(cat)",
      "command(head)",
      "command(tail)",
      "command(wc)",
      "command(rg)",
      "command(fd)",
      "command(grep)",
      "command(find)",
      "command(tree)",
      "command(file)",
      "command(sed)",
      "command(awk)",
      "command(sort)",
      "command(uniq)",
      "command(cut)",
      "command(diff)",
      "command(jq)",
      "command(realpath)",
      "command(basename)",
      "command(dirname)",
      "command(stat)",
      "command(echo)",
      "command(which)",
      "command(env)",
      "command(date)",
      "command(pnpm run check)",
      "command(pnpm run test)",
      "command(pnpm run lint)",
      "command(pnpm run typecheck)",
      "command(pnpm exec tsc)",
      "command(pytest)",
      "command(ruff)",
      "command(shellcheck)",
      "command(node)",
      "command(python3)",
      "command(pnpm run check:python)"
    ],
    "deny": [
      "command(gh)",
      "command(git push)",
      "command(git commit)",
      "command(git checkout)",
      "command(git switch)",
      "command(git reset)",
      "command(git rebase)",
      "command(git merge)",
      "command(git worktree)",
      "command(git branch)",
      "command(git tag)",
      "command(git remote)",
      "command(git clean)",
      "command(git gc)",
      "command(rm)",
      "command(mv)",
      "command(curl)",
      "command(wget)",
      "command(ssh)",
      "command(scp)",
      "command(docker)",
      "command(supabase)",
      "command(bd)",
      "command(pnpm run db:reset)",
      "command(pnpm run preflight)",
      "command(pnpm run e2e:all)",
      "command(pnpm run e2e:full)"
    ]
  }
}
