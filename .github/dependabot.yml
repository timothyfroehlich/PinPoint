version: 2
updates:
  # npm dependencies
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    target-branch: "main"
    versioning-strategy: increase
    open-pull-requests-limit: 5
    rebase-strategy: "auto"
    commit-message:
      prefix: "chore(deps)"
      prefix-development: "chore(deps)"
      include: "scope"
    # Cooldown configuration to avoid overwhelming with updates
    cooldown:
      # 2 weeks for minor and patch updates
      - duration: 14
        update-types:
          - "minor"
          - "patch"
      # 1 month (30 days) for major version updates
      - duration: 30
        update-types:
          - "major"
    groups:
      # Group minor and patch updates together
      minor-patch-updates:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
      # Group security updates separately for immediate attention
      security-updates:
        applies-to: security-updates
        update-types:
          - "major"
          - "minor"
          - "patch"
    labels:
      - "dependencies"
      - "npm"

  # GitHub Actions (when workflows are activated)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    versioning-strategy: increase
    open-pull-requests-limit: 5
    rebase-strategy: "auto"
    commit-message:
      prefix: "chore(deps)"
      prefix-development: "chore(deps)"
      include: "scope"
    # Cooldown configuration to avoid overwhelming with updates
    cooldown:
      # 2 weeks for minor and patch updates
      - duration: 14
        update-types:
          - "minor"
          - "patch"
      # 1 month (30 days) for major version updates
      - duration: 30
        update-types:
          - "major"
    labels:
      - "dependencies"
      - "github-actions"
    reviewers:
      - "timothyfroehlich"
