name: PR Screenshots (on-demand)

on:
  issue_comment:
    types: [created]

permissions: read-all

jobs:
  screenshots:
    name: PR Screenshots
    runs-on: ubuntu-latest
    if: >-
      github.event.issue.pull_request &&
      contains(github.event.comment.body, '!screenshots') &&
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
    concurrency:
      group: gh-pages
      cancel-in-progress: false
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@58077d3c7e43986b6b15fba718e8ea69e387dfcc  # ratchet:step-security/harden-runner@v2.15.1
        with:
          egress-policy: audit

      - name: React to comment
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          COMMENT_ID: ${{ github.event.comment.id }}
        run: |
          gh api "repos/${REPO}/issues/comments/${COMMENT_ID}/reactions" \
            -f content=eyes

      - name: Get PR head SHA
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.issue.number }}
        run: |
          PR_DATA=$(gh api "repos/${REPO}/pulls/${PR_NUMBER}")
          echo "sha=$(echo "$PR_DATA" | jq -r .head.sha)" >> "$GITHUB_OUTPUT"
          echo "ref=$(echo "$PR_DATA" | jq -r .head.ref)" >> "$GITHUB_OUTPUT"

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # ratchet:actions/checkout@v6.0.2
        with:
          persist-credentials: false
          ref: ${{ steps.pr.outputs.sha }}

      - name: Setup pnpm
        uses: pnpm/action-setup@c5ba7f7862a0f64c1b1a05fbac13e0b8e86ba08c  # ratchet:pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f  # ratchet:actions/setup-node@v6.3.0
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Restore node_modules cache
        id: cache-restore
        uses: actions/cache/restore@668228422ae6a00e4ad889ee87cd7109ec5666a7  # ratchet:actions/cache/restore@v5.0.4
        with:
          path: node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install dependencies (if cache miss)
        if: steps.cache-restore.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@b60b5899c73b63a2d2d651b1e90db8d4c9392f51  # ratchet:supabase/setup-cli@v1.6.0
        with:
          version: latest

      - name: Cache Playwright browsers (chromium + webkit)
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7  # ratchet:actions/cache@v5.0.4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: >-
            ${{ runner.os }}-playwright-chromium-webkit-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-playwright-chromium-webkit-

      - name: Install Playwright system dependencies
        run: pnpm exec playwright install-deps chromium webkit

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: pnpm exec playwright install chromium webkit

      - name: Generate config.toml from template
        run: cp supabase/config.toml.template supabase/config.toml

      - name: Make E2E script executable
        run: chmod +x ./scripts/supabase-init-for-tests.sh

      - name: Initialize Supabase
        run: ./scripts/supabase-init-for-tests.sh

      - name: Capture screenshots
        env:
          MOCK_BLOB_STORAGE: "true"
          PLAYWRIGHT_STDOUT: "pipe"
          PLAYWRIGHT_STDERR: "pipe"
        run: pnpm run screenshots || true

      - name: Deploy report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d475ec49e8ef4d45e  # ratchet:peaceiris/actions-gh-pages@v4.0.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./playwright-report
          destination_dir: >-
            pr-${{ github.event.issue.number }}
          keep_files: true

      - name: Comment on PR with report link
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.issue.number }}
          REPO: ${{ github.repository }}
        run: |
          OWNER="${REPO%%/*}"
          REPORT_URL="https://${OWNER}.github.io/${REPO##*/}/pr-${PR_NUMBER}/"
          COMMENT_BODY="<!-- playwright-screenshots -->
          ## 📸 Screenshot Report

          **[View interactive Playwright report](${REPORT_URL})**

          Captured across Desktop (1280×720), iPhone 13 Mini (375×812, WebKit), and Pixel 5 viewports."

          # Find existing comment by marker
          EXISTING=$(gh api "repos/${REPO}/issues/${PR_NUMBER}/comments" \
            --jq '.[] | select(.body | contains("<!-- playwright-screenshots -->")) | .id' \
            | head -1)

          if [ -n "$EXISTING" ]; then
            gh api "repos/${REPO}/issues/comments/${EXISTING}" \
              -X PATCH -f body="$COMMENT_BODY"
          else
            gh api "repos/${REPO}/issues/${PR_NUMBER}/comments" \
              -f body="$COMMENT_BODY"
          fi
