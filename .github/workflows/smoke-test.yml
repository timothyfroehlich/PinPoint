name: Smoke Test
permissions:
  contents: read

env:
  CI: true
  NODE_ENV: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke-test:
    name: End-to-End Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      SKIP_ENV_VALIDATION: true

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Variables
        run: |
          set -e  # Exit on any error
          # Validate required secrets are present
          [[ -n "${{ secrets.VERCEL_TOKEN }}" ]] || { echo "❌ VERCEL_TOKEN secret not set"; exit 1; }
          [[ -n "${{ secrets.VERCEL_ORG_ID }}" ]] || { echo "❌ VERCEL_ORG_ID secret not set"; exit 1; }
          [[ -n "${{ secrets.VERCEL_PROJECT_ID }}" ]] || { echo "❌ VERCEL_PROJECT_ID secret not set"; exit 1; }

          # Pull environment variables
          vercel env pull .env.local --environment=preview --token=${{ secrets.VERCEL_TOKEN }}

          # Verify the .env.local file was created
          [[ -f .env.local ]] || { echo "❌ .env.local file not created"; exit 1; }
          echo "✅ Environment variables successfully pulled from Vercel"
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 1.133.3

      - name: Start Supabase Local Stack
        run: |
          set -e  # Exit on any error
          echo "🚀 Starting local Supabase stack for CI testing..."

          # Start Supabase services with optimized exclusions for CI performance
          # Excludes: studio, realtime, storage-api, edge-runtime, logflare, vector, imgproxy, supavisor, postgres-meta, mailpit
          # Keeps: PostgreSQL, PostgREST (API), GoTrue (auth), Kong (gateway) - all essential services
          supabase start -x studio,realtime,storage-api,edge-runtime,logflare,vector,imgproxy,supavisor,postgres-meta,mailpit

          # Wait for services to be ready with proper health checks
          echo "⏳ Waiting for Supabase services to be ready..."
          for i in {1..30}; do
            if curl -s --fail http://localhost:54321/health > /dev/null 2>&1; then
              echo "✅ Supabase API server is ready!"
              break
            fi
            echo "🔄 Services not ready yet, waiting... (attempt $i/30)"
            sleep 2
          done

          # Export Supabase environment variables automatically
          echo "🔧 Configuring environment variables from Supabase CLI..."
          supabase status -o env >> "$GITHUB_ENV"

          # Verify services are running
          supabase status

          echo "✅ Supabase local stack ready for testing"


      - name: Cache Playwright Browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: "~/.cache/ms-playwright"
          key: "${{ runner.os }}-playwright-chromium-${{ hashFiles('package-lock.json') }}"
          restore-keys: |
            ${{ runner.os }}-playwright-chromium-
            ${{ runner.os }}-playwright-

      - name: Install Playwright Browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium

      - name: Setup Database Schema and Seed Data
        run: |
          set -e  # Exit on any error
          echo "🗃️ Setting up database schema and seed data with local Supabase..."

          # Push schema to local Supabase database
          npm run db:push:local

          # Seed database with full auth + database users (like local dev)
          npm run db:seed:local:sb

          echo "✅ Database setup complete - ready for smoke test"

      - name: Run Smoke Test
        run: |
          set -e  # Exit on any error
          # Validate critical environment variables are set by Supabase CLI
          [[ -n "$API_URL" ]] || { echo "❌ API_URL not set by Supabase CLI"; exit 1; }
          [[ -n "$ANON_KEY" ]] || { echo "❌ ANON_KEY not set by Supabase CLI"; exit 1; }

          # Run smoke test - must pass completely for CI success
          echo "🧪 Running smoke test with local Supabase stack"
          npm run smoke

      - name: Upload Playwright Report
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: failure()
        with:
          name: playwright-report-smoke
          path: playwright-report/
          retention-days: 7
